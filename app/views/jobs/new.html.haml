= form_for(@job, data: { controller: 'jobs-new' }) do |f|
  %div
    = f.label(:name)
    = f.text_field(:name)
  %div
    = f.label(:client)
    = render JobsClientSelectComponent.new
  %div
    = f.submit

%turbo-frame#jobs-client-new{ src: new_jobs_client_path }
