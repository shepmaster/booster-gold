= form_for(@job, data: { controller: 'jobs-new' }) do |f|
  %div
    = f.label(:name)
    = f.text_field(:name)
  %div
    = f.label(:client)
    = render partial: 'select', locals: { selected: 7 }
  %div
    = f.submit

%turbo-frame#bruh{ src: new_jobs_client_path }
